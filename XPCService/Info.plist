<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>XPC!</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
    <key>NSSpeechRecognitionUsageDescription</key>
    <string>The Yooz Engine XPC service uses Apple's on-device Speech Recognition as an optional STT backend. Transcription runs locally; audio never leaves your device.</string>
    <!--
        Team-ID-prefixed app group (engine#227 "Weights/app-group wiring").
        Xcode substitutes $(TeamIdentifierPrefix) at build/sign time, so
        `main.swift` reads the fully-resolved group id back at runtime via
        `Bundle.main.object(forInfoDictionaryKey:)` without ever guessing its
        own team id. See AppGroupWeightsLocation.swift + CONSUMER_INTEGRATION.md
        "Weights/app-group wiring" for the full convention.
    -->
    <key>YoozAppGroupIdentifier</key>
    <string>$(TeamIdentifierPrefix)live.yooz.engine.shared</string>
    <key>XPCService</key>
    <dict>
        <key>ServiceType</key>
        <string>Application</string>
    </dict>
</dict>
</plist>
